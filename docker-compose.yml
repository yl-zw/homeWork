
services:
  mysql:
    image: mysql:8.0
    restart: always
    command:
      - --default-authentication-plugin=mysql_native_password
      - -d
    ports:
      - 3306:3306
    environment:
      MYSQL_ROOT_PASSWORD: root






